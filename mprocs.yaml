procs:
  auth:
    cwd: "apps/auth"
    shell: "pnpm run dev"
  api:
    shell: "cargo run"
  web:
    cwd: "apps/web"
    shell: "pnpm run dev"
  og:
    cwd: "packages/og"
    shell: "pnpm run dev"
