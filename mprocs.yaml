procs:
  auth:
    cwd: "apps/auth"
    shell: "pnpm run dev"
  api:
    shell: "cargo watch -x run"
  web:
    cwd: "apps/website"
    shell: "pnpm run dev"
  og:
    cwd: "packages/og"
    shell: "pnpm run dev"
