procs:
  auth:
    cwd: "apps/auth"
    shell: "pnpm run dev"
  api:
    shell: "bacon run"
  web:
    cwd: "apps/web"
    shell: "pnpm run dev"
  og:
    cwd: "packages/og"
    shell: "pnpm run dev"
